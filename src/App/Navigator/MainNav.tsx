/**
 * Main Nav is the base class for exporting the Whole Navigation flow
 * Tried to create a structured application flow
 */

import React from 'react';
import DrawerNav from './DrawerNav';

const MainNav = () => {
  return <DrawerNav />;
};

export default MainNav;
